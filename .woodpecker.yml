labels:
  device: mimir

steps:
  - name: uname
    image: alpine
    commands:
      - echo "Running test on Mimir"
      - uname -a
    when:
      labels:
        linux: arm64
      event:
        - push